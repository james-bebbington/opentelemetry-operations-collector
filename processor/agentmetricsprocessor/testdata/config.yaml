receivers:
  examplereceiver:

processors:
  hostmetricsstackdriver:

exporters:
  exampleexporter:

service:
  pipelines:
    metrics:
      receivers: [examplereceiver]
      processors: [hostmetricsstackdriver]
      exporters: [exampleexporter]
